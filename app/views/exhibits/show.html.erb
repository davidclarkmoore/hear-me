<p id="notice"><%= notice %></p>

<%= link_to 'Back', exhibits_index_path %>

<p>
  <strong>Name:</strong>
  <%= @exhibit.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @exhibit.description %>
</p>

<%= link_to 'Edit', edit_exhibit_path(@exhibit) %> |
<%= link_to 'Back', exhibits_path %>
