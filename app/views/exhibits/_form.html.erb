<%= simple_form_for @exhibit do |f| %>
  <% if @exhibit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exhibit.errors.count, "error") %> prohibited this exhibit from being saved:</h2>

      <ul>
      <% @exhibit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.input :name, label: 'Name:', error: 'Whoops! We need a name here :)' %>
  <%= f.input :description, label: 'Description:', error: 'Whoops! We need a description here :)' %>
  <%= f.association :organization %>
  <%= f.file_field :image, label: 'Logo Image:', error: 'Whoops! We need an image here :)' %>
  <%= f.button :submit %>
<% end %>
